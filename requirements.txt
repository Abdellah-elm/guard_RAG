# Pinned to minimum known-good majors; run `pip install -U -r requirements.txt`
# and re-pin exact versions once the Phase 1 retrieval code is written —
# don't trust these patch versions blindly, check PyPI for current releases.
fastapi>=0.115
uvicorn[standard]>=0.32
qdrant-client>=1.12
redis>=5.2
sentence-transformers>=3.3
python-dotenv>=1.0
anthropic>=0.40
langfuse>=2.50
pydantic>=2.10
